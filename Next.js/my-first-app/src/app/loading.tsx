export default function CatalogLoading() {
    return <p>Carregando catálogo...</p>
    // arquivo loading ele roda enquanto o arquivo page.tsx não carrega
    // ele herda o cabeçalho

    // esse arquivo serve para todas as paginas filhas do caminho /app a não ser que eu sobrescreva
  }