NEXT_PUBLIC_API_BASE_URL= "http://localhost:3000"
APP_URL="http://localhost:3000" # não preciso usar o prefixo NEXT_PUBLIC pq a vairavel é usada numa aplicação do lado do servidor
#   o next_public faz o next ver na parte do servidor do next (rotas api por exemplo) e do cliente (componentes)
